---
import type { User } from "lucia";
import GithubProfile from "./profile/github_profile.astro";
import Username from "./profile/username.astro";

interface Props {
  user: User;
}

const { user } = Astro.props;
---

{user.githubId ? <GithubProfile user={user} /> : <Username user={user} />}
