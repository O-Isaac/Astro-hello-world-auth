---
import type { User } from "lucia";
import Profile from "./profile.astro";

interface Props {
  user: User | null;
}

const { user } = Astro.props;
---

{
  user ? (
    <Profile user={user} />
  ) : (
    <a class="py-2" href="/signin">
      Iniciar Sesión
    </a>
  )
}
